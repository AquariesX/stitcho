// Backward-compatible alias. Both web and Flutter order creation use the
// same compatibility validation, Decimal pricing, and snapshot persistence.
export { GET, POST } from "@/app/api/mobile/orders/route";
